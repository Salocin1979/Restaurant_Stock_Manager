<h1 class="text-2xl font-bold">All Categories</h1>
<ul class="mt-4 space-y-2">
  <% @categories.each do |category| %>
    <li>
      <%= link_to category.name, category_path(category), class: "text-blue-600 hover:text-blue-800" %>
    </li>
  <% end %>
</ul>
