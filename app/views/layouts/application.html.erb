<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <title><%= content_for(:title) || "Restaurant Stock Manager" %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">

    <!-- Favicon -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Tailwind CSS and other stylesheets -->
    <%= stylesheet_link_tag "application.tailwind", "data-turbo-track": "reload" %>


    <!-- Include JavaScript (Turbo + Stimulus) -->

    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-100 text-gray-900 font-sans antialiased">

    <!-- Navbar / Header -->
    <header class="bg-blue-600 text-white shadow-md">
      <div class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          <h1 class="text-2xl font-semibold">Restaurant Stock Manager</h1>
          <nav class="space-x-6">
            <%= link_to "Accueil", root_path, class: "hover:text-blue-300" %>
            <%= link_to "Catégories", categories_path, class: "hover:text-blue-300" %>
            <%#<%= link_to "Inventaire", inventory_path, class: "hover:text-blue-300" %>
            <%= link_to "Se connecter", new_user_session_path, class: "hover:text-blue-300" %>
          </nav>
        </div>
      </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto mt-10 px-5 flex flex-col space-y-8">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-blue-600 text-white mt-16 py-6">
      <div class="container mx-auto text-center">
        <p>&copy; <%= Time.now.year %> Restaurant Stock Manager. Tous droits réservés.</p>
      </div>
    </footer>

  </body>
</html>
